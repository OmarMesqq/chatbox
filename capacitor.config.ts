import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'xyz.zeroxchat.sh',
  appName: 'Zerox Chat',
  webDir: 'release/app/dist/renderer'
};

export default config;
